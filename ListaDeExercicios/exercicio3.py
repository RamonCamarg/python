# Crie uma lista com três cores diferentes
cores = ["azul", "vermelho", "verde"]

# Adicione mais duas cores à lista
cores.append("amarelo")
cores.append("roxo")

# Imprima a lista de cores
print("Lista de cores:", cores)

# Declare uma tupla com as coordenadas (latitude, longitude) de um local
coordenadas = (40.7128, 74.0060)

# Imprima cada valor da tupla separadamente
latitude, longitude = coordenadas
print("Latitude:", latitude)
print("Longitude:", longitude)
